version: '3'

services:
    api:
        build: ./
        volumes:
            - .:/app
        ports:
            - '8200:8200'
        depends_on:
            - mongo
        tty: true
    mongo:
        image: mongo
        ports:
        - "27017:27017"
