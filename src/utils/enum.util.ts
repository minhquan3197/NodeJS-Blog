export enum IS_ADMIN {
    INACTIVE = 0,
    ACTIVE = 1,
}

export enum ENVIRONMENT_TYPE {
    TEST = 'test',
    PROD = 'production',
    DEV = 'development',
}

export enum BLOG_STATUS {
    INACTIVE = 0,
    ACTIVE = 1,
}
